#include<iostream>
#include<vector>
using namespace std;
int main(){
    vector<int>arr=[3,0,1];
    int n=arr.size();
    sort(nums.begin(),nums.end());
    for(int i=0;i<n;i++){
        if(i != arr[i]){
            return i;
        }
    }
    return 0;   
}